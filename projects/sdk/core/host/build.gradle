apply plugin: 'java-library'

group 'com.tencent.shadow.core'

dependencies {
    api files("${project(":host:aar").getBuildDir()}/outputs/jar/aar-release.jar")
}

compileJava.dependsOn(":host:aar:jarReleasePackage")